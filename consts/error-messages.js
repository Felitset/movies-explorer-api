const incorrectLoginPassword = 'Wrong login or password';

const emailDuplicate = 'Пользователь с таким имейлом существует';

const invalidEmail = 'Введен некорректный email';

const serverError = 'На сервере произошла ошибка';

const pageNotFound = 'Page not found';

const dbValidationError = 'Error DB validation';

const nonExistingMovie = 'Фильм не существует';

const unableToDelete = 'Невозможно удалить не свою карточку';

const wrongData = 'WrongData';

const userDuplicateError = 'User duplicate';

const noIdMatch = 'Нет пользователя с таким id';

const userNotFound = 'Пользователь не найден';

module.exports = {
  incorrectLoginPassword,
  emailDuplicate,
  invalidEmail,
  serverError,
  pageNotFound,
  dbValidationError,
  nonExistingMovie,
  unableToDelete,
  wrongData,
  userDuplicateError,
  noIdMatch,
  userNotFound,
};
